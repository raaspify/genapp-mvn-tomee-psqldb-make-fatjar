run as mvn clean package. This builds fat jar from the app source for a specific appserver target
the output is in the target dir and among other contains ROOT-exec.jar
There are batch files as convenience to invoke run as mvn clean package

target dir to house all output of the build.

pom build directory structure
tbd
